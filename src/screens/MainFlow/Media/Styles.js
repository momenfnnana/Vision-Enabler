import Color from '@Assets/Constant'

const styles = {
    container: {
        flex: 1,
        width: "100%",
        backgroundColor: Color.white
    },
    image: {
        position: "absolute",
        top: 0,
        right: 0,
        resizeMode: "contain"
    }
}
export default styles